import React,{Component} from 'react'

class BookList extends Component{
    constructor(){
        super()
        this.state = {

        }
    }

    render(){
        const mappedBooks = this.props.map((element, index) => {
            return <
        })
    }
}

export default BookList